import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
        backgroundColor: '#21aaf1',
        padding: 16,
        width: '100%',
        borderRadius: 8,
        color: '#fff',
        textAlign: 'center',
    },
});

export { styles };