import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    buttonSolid: {
        backgroundColor: '#21aaf1',
        padding: 16,
        width: '100%',
        borderRadius: 12,
        color: '#fff',
        textAlign: 'center',
    },
    buttonOutline: {
        backgroundColor: '#fff',
        padding: 16,
        width: '100%',
        borderRadius: 12,
        borderWidth: 2,
        borderColor: '#21aaf1',
        textAlign: 'center',
    },
});

export { styles };
