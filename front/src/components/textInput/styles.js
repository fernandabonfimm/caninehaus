import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        width: '100%',
        padding: 16,
        borderColor: '#F1F5F5',
        borderWidth: 1,
        backgroundColor: '#fff',
        color: '#546E7A'
    },
});

export { styles };