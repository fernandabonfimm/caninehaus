import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    flex: 1,
    padding: 16,
    borderColor: "#F1F5F5",
    borderWidth: 2,
    borderRadius: 12,
    backgroundColor: "#fff",
    color: "#546E7A",
  },
  passwordInputContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  iconContainer: {
    position: "absolute",
    right: 16,
  },
});

export { styles };
